language: node_js
node_js:
  - "8"
  - "9"
  - "10"
sudo: false
after_script: npm run coverage
services:
  - redis-server
cache:
  directories:
    - node_modules
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
